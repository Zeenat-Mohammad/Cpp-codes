#include <iostream>
using namespace std;
int sum(int a, int b) {
    while (b != 0) { 
        int carry = (a & b) << 1; 
        a = a ^ b;
        b = carry; 
    } 
    return a;
} 
int main() {
    int a = -1, b = 2;
    cout << sum(a, b);
    return 0; 
}
