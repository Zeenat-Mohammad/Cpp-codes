#include <iostream>
using namespace std;

int main(){
    int num,copy,count=0;
    cout<<"Enter a number: ";
    cin>>num;
    copy=num;
    while(num!=0){
        count++;
        num/=10;
    }
    cout<<copy<<" contains "<<count<<" digits";
    return 0;
}
